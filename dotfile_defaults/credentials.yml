# ~/.olive/credentials.yml
#
# Define API keys for use with Olive.
# Olive will pick the correct key based on `ai.provider` in preferences.yml.

openai:
  api_key: sk-...

ollama:
  api_key: mock  # placeholder, not used but must be present
